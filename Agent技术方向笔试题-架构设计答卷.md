# Agent 技术方向笔试题

## 第二部分：架构设计题

> 共 5 个模块，**每个模块选择一道题目回答**。

### 模块一：Context / Performance

2. 一个 Session 已经连续对话 200 轮，Context 即将达到上限。你会如何进行压缩？如何确保压缩后的对话仍然流畅？

#### 回答


## 一、根据产品、使用场景和用户需求设计压缩策略

不同场景下，Context 的增长方式并不一样。普通聊天可能经过很长的对话才接近窗口上限，而 Coding Agent 的文件读取、日志和 Tool Result 会迅速占用 Context，几轮交互就可能接近上限。题目中的长对话场景，与 Coding Agent 的实际使用场景可能有很大差异。

因此，压缩策略应根据不同产品、使用场景和用户需求设计，决定哪些信息优先保留、如何压缩以及何时触发。

## 二、分别保护历史背景与当前意图

Context 可以笼统分为System Prompt **History Context**（当前 Turn 之前的历史）和 **Active Turn**（最新用户消息及本轮执行过程），二者承担不同的保留目标。

History Context 需要保证：

- **语义连续性**：保留人物、对象、指代、话题、事件的时间与因果关系，以及用户态度、偏好、情绪和未结束的话题，使 Agent 能接上对话。
- **任务一致性**：保留目标、约束、已确认决策、当前状态、已完成动作、未解决问题、下一步和完成标准，避免任务偏离、重复执行或恢复已废弃方案。

两者的权重随业务变化：陪伴聊天更重视人物关系、情绪和指代；Coding Agent 与长程任务更重视目标、约束和执行状态；高风险操作还必须精确保留金额、时间、对象、授权与确认，并附原始消息引用。因此，可采用通用 Context Snapshot，再按业务调整字段和保留比例。

Active Turn 则单独保证**当前意图保真**，不归入上述两个历史保留目标。最新要求可能继续、修改、否定或替换旧任务，应具有更高的保留优先级，不能混入旧历史摘要。

应同时保留用户消息原文与结构化意图，记录动作、对象、预期结果、新增约束、与旧任务的关系，以及已完成工具调用、关键结果、当前动作和待执行事项。原文与意图冲突时，应根据原文重新解析。

例如“Redis 方案不要了，改成本地缓存，但公开 API 保持不变，继续做”，必须同时保留方案变更、API 约束和继续任务的要求，避免旧决策覆盖新意图。

## 三、按信息类型决定保留、压缩与外置

**不能有损压缩，不等于必须永久放在 Context 中。** 精确金额可以无损存入数据库，需要时再召回；代码和日志也可外置，保留关键片段与引用。

分类时综合考虑精度、错误成本、当前相关性、可恢复性、稳定性、关系性、来源权威性与体积，再决定如何表示、存放在哪里以及何时召回。

存储方式按信息特点选择：复杂且稳定的关系适合图存储，时间顺序适合事件流，语义相似性适合向量检索，精确业务事实适合关系数据库或 KV；知识图谱不应成为默认选择。

信息优先级也会随任务变化：错误日志在排查时保留关键原文，原因确认后压缩为结论，任务结束后只保留外部引用。相关信息可提升到当前工作集，已完成过程可降级；旧决策被替换后标记为 `superseded`，动态事实记录更新时间或 TTL，Agent 推测与用户确认、工具事实分开保存。

最终 Context 组合以下内容：**当前原文与意图、最近若干轮原文、历史结构化状态、自然语言语义摘要、原始历史引用**。它们分别承接最新要求、即时指代与语气、任务状态、关系与因果，以及细节恢复。

## 四、通过原始记录支持按需恢复

摘要是有损的，不能成为唯一事实来源。应将用户消息、Assistant Action、Tool Call、Tool Result 和状态变化持久化为追加式 Event Log，并在 Snapshot 中保留来源引用。

引用使用稳定的 `session_id`、`turn_id`、`event_id`、`checkpoint_id`、`artifact_id` 和来源事件范围，避免只依赖可能因合并、迁移或并发写入而变化的 JSONL 行号。

当指代无法解析、结论缺少依据、状态冲突、用户质疑记忆、需要精确细节或即将执行高风险操作时，Agent 可通过历史检索工具补回相关原文。检索可结合 Event ID、时间、关键词、语义与实体过滤。

结构化摘要是快速路径，Event Log 是事实来源，历史检索是信息不足时的恢复路径；日常使用摘要，必要时恢复相关片段。

## 五、在三个检查点执行压缩与恢复

1. **Pre-turn：第一次 LLM Dispatch 前。** 用户消息到达后检查预算，必要时压缩旧 History，并将 System Prompt、历史摘要、最近原文和当前原始消息与意图重新组装。最新消息不能混入旧摘要。
2. **Agent Loop：每次 LLM Dispatch 前。** 工具结果可能很大，不能只在 Turn 开始时检查。压缩时保护整个 Active Turn 的意图和执行状态，而非只保留最后一个结果。完整工具结果可以外置，Context 保留工具名称、参数、关键结果与引用。沿用原始 Function Calling 协议时，调用与结果必须合法配对；重建 Context 时，已完成调用可以转成结构化 Active Turn State。
3. **Context Overflow Recovery：兜底恢复。** 从 Event Log 和最后成功持久化的 Checkpoint 恢复，重建当前原文与意图，保留已完成调用及关键结果，对更早历史进行更激进的压缩，再从失败的 LLM Dispatch 重试。恢复边界是最后成功持久化的事件，不能简单退回“上一轮”；有副作用的工具需要幂等键，避免重复执行。

## 六、用 Evals 决定具体策略

压缩 Schema、保留字段与比例、触发阈值和召回策略，需要通过不同业务的 Evals 确定。

评估应覆盖意图还原、约束保留、指代解析、长程任务完成、工具选择与参数正确性、历史检索命中、高风险事实核验、Overflow 发生率，以及 Token 成本、首 Token 延迟和整体响应时间。

据此决定最近原文保留多少、哪些信息必须逐字保留、工具结果何时外置、何时主动检索、各检查点采用什么阈值，以及 Overflow 时的压缩强度。

压缩可以舍弃可恢复的历史细节，但不能改写当前意图、破坏任务状态，也不能让 Agent 在信息不足时依靠猜测。

---

### 模块二：Memory

2. 你所理解的 Agent Memory 经典框架是什么？它的发展趋势是什么？行业头部玩家正在采用哪些方案？

#### 回答



我理解的 Agent Memory 经典框架不是某个具体项目，而是一条从原始经历到记忆形成、再到记忆使用的完整链路。我会借用数据工程里的概念，把它概括为 **ETL + Retrieval**，最后再加上执行反馈形成闭环。

这里的 ETL 不是简单做一次数据搬运。**Extract** 是从对话、工具调用和任务轨迹中识别什么值得记，同时保留来源和时间；**Transform** 是把原始内容清洗、去重、关联和抽象，转化成系统需要的记忆形态，例如事实、事件、用户画像、Agent 案例或技能；**Load** 是把这些产物按用户、Agent、项目等作用域写入长期存储，并建立全文、向量或图索引。严格来说，检索不属于 ETL 的 Load：Load 是“把记忆装进记忆系统”，Retrieval 才是“在后续任务中把相关记忆取出来”，经过过滤、排序和上下文组装后提供给 Agent。Agent 的执行结果又会成为新的输入，触发下一轮提取和更新。

从行业方案看，我认为现在有三个比较明显的分化方向。

第一，**Transform 的产物正在从通用条目走向分层、分类型组织。** 以 Mem0 为例，它更像把内容提取成可以独立理解的原子条目，例如事实、偏好和计划。这些条目内容上有差别，但通常共用一条普通记忆管线。EverOS 则进一步采用分层、分轨的组织方式：先保留 MemCell 原始片段，再形成 episode；用户侧继续形成 atomic fact、foresight 和 profile，Agent 侧形成 case，再从相似 case 中归纳 skill。这样 profile 或 skill 可以回到对应的 episode 和原始证据。它反映的趋势是，记忆正在从扁平条目变成“原始材料—事件经历—派生认识—抽象能力”的层次结构，而且用户记忆和 Agent 记忆会采用不同的更新逻辑。

第二，**Load 的组织方式与 Retrieval 的检索方式正在协同分化。** Load 不只是把记忆写入数据库，还要根据后续使用方式，把记忆组织到全文、向量、图和时间等可访问的数据结构中；它建立了系统可用的召回通道，Retrieval 再根据当前任务选择并组合这些通道。一些系统在 Load 阶段主要为独立记忆条目建立关键词和向量索引，因此 Retrieval 主要解决“哪些内容与当前问题精确匹配或语义相似”。Nowledge Mem 这类方案则在 Transform 阶段进一步提取实体及知识之间的替代、补充、确认或冲突关系，并在 Load 阶段保存这些节点、关系和时间信息，因此读取时不仅可以做全文和向量检索，还可以沿图关系找到相关人物、事件、来源和版本链。未来更可能是关键词、向量、实体图和时间信号的混合检索，而不是只押注一种方法。

第三，**竞争正在从记忆应用层下沉到存储和运行时。** 当系统需要处理版本、来源、时间关系、并发写入和一致性时，直接拼装社区数据库未必总能满足产品要求，所以有些厂商会投入专用的数据引擎。Zep 已明确披露托管版使用专有的 Context Graph Engine；Nowledge Mem 也在重写原生引擎，但仅凭公开资料还不能断言它已经自研了完整数据库内核。因此更准确的趋势不是“所有厂商都会自研数据库”，而是头部方案会围绕自己的记忆模型，把数据层和运行时做得更深、更紧。

所以我判断，Agent Memory 接下来的核心竞争有三点：Transform 最终形成什么样的记忆单元，Retrieval 如何准确取回并组装上下文，以及数据层能否可靠地支撑长期演化。最终标准不是存了多少条，而是 Agent 是否少犯重复错误、减少用户纠正，并在合理成本下把后续任务做得更好。

---

### 模块三：Task

1. 对于长程任务，大模型执行一段时间后可能会忘记目标。你知道哪些解决方案？各自有什么优缺点？

#### 回答



“忘记目标”可能有不同原因：目标和约束被大量工具结果淹没；压缩或跨 Session 交接丢失状态；模型陷入局部问题，偏离总目标；或者新证据出现后仍在执行旧计划。常见方案有以下三类，它们的收益和代价各不相同。

**1. 在 Agent Loop 外保存 Goal 和共享任务状态**

基础做法是持久化目标及运行状态，由外层控制逻辑在一次 Run 结束后判断是否续跑，续跑不要求新建 Agent 或清空上下文。在此基础上，还可以扩展为共享任务状态，记录约束、验收标准、进展、关键决策和失败尝试。

优势是保持跨轮次的任务连续性，减少重复探索，为暂停、恢复和验收提供依据。代价是状态维护和同步成本：记录太少容易遗漏信息，记录太多会增加维护负担；状态过时或记录有误还会误导后续执行。过度限定状态结构或写死领域流程也可能限制泛化。只有目标和状态，并不能自动解决连续无效尝试或死循环。

**2. 用 Todo 和滚动规划维护近期进度**

通过 Todo 工具显式记录待办及状态，由 Harness（执行框架）按策略回放当前清单，或让模型主动读取。优势是让当前事项和进度可见，帮助检查遗漏；代价是维护清单和提醒的 Token 开销，以及过时计划被反复强化的风险。滚动规划只细化近期动作，能降低过早固定方案的风险，但也增加了持续调整计划的成本。Todo 本身不负责 Run 结束后的自动续跑。

需要区分行为变化与结果改善。[Apache Maka PR #561](https://github.com/apache/maka/pull/561) 的公开实验中，`todo_write` 配合引导和状态回放，在 7/8 次尝试中被调用，并出现持续维护清单的行为；但该组通过数仅从对照组的 4/8 变为 5/8，成本增加 18.8%，还存在清单完成而验证失败的案例。小样本和轨迹缺失限制了结论，而且测试的是工具与引导的组合。因此，不能把清单更新更有规律直接当作稳定性、实现质量或成功率提高的证据。

**3. 用 Subagent / Handoff 隔离或交接执行上下文**

这两种方式都把任务及相关背景传递到另一个执行上下文。Subagent 承接搜索、排查等局部任务，返回结论和证据，主 Agent 继续负责整体推进；Handoff 则通过交接包，让新的 Session 接续主任务。它们可以减少单个上下文的负担，代价是信息损失、理解恢复和结果整合成本。

两者共同的风险是继承主 Agent 的错误理解或判断。如果主 Agent 已经误解需求、判断错了问题原因，却把这些判断作为委派或交接的前提，后续 Agent 就可能沿着错误方向继续执行。即使信息传递完整，错误也可能被持续保留；多个执行者沿用同一前提得到一致结论，也不能证明这个前提正确。

这类方案的权衡在于信息传递的完整性与上下文开销：传得太少容易丢失约束和依据，传得太多又会保留原有负担。保留原始需求和证据有助于核验错误前提，但也会增加后续读取与验证的成本。

---

### 模块四：Tool / Session Runtime

1. Agent 工具有同步和异步两类。异步工具不能让用户一直等待，但其执行结果仍然重要。你会如何设计异步工具的执行和完成通知机制？

#### 回答

##
我会把异步工具分为交互型和结果型，并让它们共用任务管理和通知底座。

交互型工具的中间状态会影响决策，例如终端、和可复用子 Agent。我会先返回带操作句柄的 tool result，包含句柄、当前状态和可用操作；同时额外设计状态查询、增量输出、输入、继续和取消接口，让模型在需要干预时操作。就绪、需要输入、失败和完成等关键事件也可以主动通知。

结果型工具例如检索、下载和报告生成，模型通常只关心最终产物。我会先返回占位型 tool result，明确“任务已受理、仍在后台执行，完成后自动通知”，使启动调用及时返回，父 Agent 可以继续处理其他事情。Host 持续管理执行，完成后再将摘要与结果引用投递到父 Agent 的消息队列，必要时恢复空闲的父 Agent。长结果按引用读取，不要求模型反复查询占位回执，也不把全部日志塞进上下文。

Codex 后台命令通过 `session_id + write_stdin` 提供交互和结果领取。它的 subagent 同样具有 interactive 性质：先通过 `spawn_agent` 创建并返回子 Agent 身份，再通过 `send_message` 补充信息或调整方向、`followup_task` 继续派发任务，并配合状态查看、中断和等待工具持续交互；任务完成后，再通过 mailbox 主动回传最终回答。Kimi 则将 Bash 和 subagent 接入统一 Task 管理器，后台完成后产生可调度的通知。因此，应先判断模型是否具备调用 interactive 工具的能力，再结合任务语义与具体场景来设计交互接口和结果投递策略。

运行时还需要保证任务不依附当前工具调用栈、完成结果先保存、通知可去重重投、取消与重试语义明确，并把 UI 通知、模型上下文交付和空闲唤醒分开。模型能力影响接口是否好用，但可靠执行与结果不丢失应由 Harness 保证。

---

### 模块五：Agent Runtime 架构对比

1. Claude Code 的工具输出方式，与国内 GLM、豆包等使用的 OpenAI-compatible Function Calling 有什么不同？这些设计各自有什么优缺点？

#### 回答

# 工具调用 API 的区别与优缺点

Claude Code 与模型交互时使用的 Anthropic Messages API，以及 OpenAI 的 Chat Completions、Responses API，可以沿着一次工具调用的过程来比较：先声明工具和调用范围，再接收模型生成的调用，最后执行工具并返回结果。

## 一、先把工具交给模型：调用范围与缓存

三种 API 都通过 `tools` 提供工具名称、描述和参数定义。区别在于，声明了很多工具后，怎样限制本轮只调用其中一部分。

比如一套工具既能读文件，也能写文件，但这一轮只允许读取。Messages 的普通自定义工具通常需要客户端过滤 `tools`，只提交允许调用的工具。这样比较直接，也能减少上下文占用；不过，每轮增删工具会改变提示词前缀，影响缓存命中。
claude messages api则会裁剪掉工具，破坏提示词前缀，导致缓存破坏。
当前 OpenAI 两种 API 都提供 `allowed_tools`，可以保留完整的工具定义，只调整本轮的可调用集合，更有利于复用缓存。

这里还要看具体平台：兼容 OpenAI 格式，不代表支持全部配置。推理强度等设置也可能影响缓存，claude就把思考强度的token放在提示词前面，所以每当切换思考强度时，缓存命中率就会被破坏，而gpt5.6还有gpt astra 模型就不会破坏缓存命中。实际影响取决于模型，不能只看 JSON 字段的位置判断。

## 二、模型决定调用工具后：输出结构与流式处理

**Messages API** 把文本、思考和工具调用放在有序的 `content[]` 中。工具调用是 `tool_use` 块，完整响应里的参数 `input` 已经是对象，应用拿来使用比较方便。不过，流式传输时，参数仍然是逐段到达的 JSON 文本，需要在 `content_block_stop` 后完成组装。因此，对象格式主要改善易用性，不能据此判断性能更好。[1]

**Chat Completions** 把文本放在 assistant 的 `content`，工具调用放在 `tool_calls[]`。参数 `arguments` 是 JSON 字符串，应用需要额外解析。它也支持边接收边按索引拼接调用，但标准流没有逐个调用的结束事件，通常等到 `finish_reason="tool_calls"` 再执行。它的优势是接口普及、适配方便，不足是单个调用的完成边界不如另外两种清晰。[2]

**Responses** 则把消息、推理和函数调用分别作为 item，放进有序的 `output[]`。参数同样需要 JSON 解析，但每个 item 有自己的事件边界：`response.output_item.done` 表示这个 item 已完成，`response.completed` 才表示整次响应结束。应用可以先执行已完整生成的调用，同时继续接收后续输出，代价是要处理更多事件和状态。Anthropic 也有块级完成边界，所以这一点并非 Responses 独有。[1][3]

## 三、拿到调用后：工具执行与服务端托管
Responses 还提供服务端搜索、会话续接和上下文压缩。采用这些能力后，应用侧 Agent Harness 要自己维护的逻辑会减少，开发更省事；相应地，对服务商的依赖更强，对托管功能内部细节的控制也更少。


