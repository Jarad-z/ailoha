# 长程任务的目标保持方案

> 模块三：Task，第 1 题——对于长程任务，大模型执行一段时间后可能会忘记目标。你知道哪些解决方案？各自有什么优缺点？

长程任务中的“忘记目标”有几种表现：最初的目标和约束被大量工具结果淹没；上下文压缩或跨 Session 交接时丢失关键信息；模型过度关注眼前的局部问题，逐渐扩大任务范围；或者继续执行已经不适用的旧计划。

相关方案可以分为三类：**外部目标与任务状态、Todo 与计划提醒、Subagent 与 Handoff。** 它们缓解的问题、带来的收益和引入的成本不同，不能仅凭“任务很长”就认定需要某一种方案或将它们全部引入。

## 一、外部目标与任务状态：保证跨执行的任务连续性

### 1. 基础方案：Goal 模式

在普通 Agent Loop 外维护一个持久目标，记录目标文本和运行状态。每次执行时，将当前目标提供给模型；本轮结束后，外层控制逻辑根据目标状态和运行条件，决定是否触发下一轮执行。

```text
保存 Goal
    ↓
提供目标及必要上下文，执行一次 Agent Run
    ↓
根据执行结果更新目标状态
    ↓
仍可继续 → 下一次执行
完成、阻塞或暂停 → 停止自动推进
```

这里的 Run 可以包含多次模型请求和工具调用。“下一次执行”不等于新建一个独立 Agent，也不等于清空上下文。Goal 可以挂在同一个会话上，沿用历史或压缩后的上下文。Codex 的 Goal 就用于跨轮次持续推进一个持久目标。[官方 Goal 说明](https://learn.chatgpt.com/use-cases/follow-goals)

**优势**是实现相对简单，目标可以独立于对话历史保存，也能避免一次正常回复结束就终止整个任务。它为续跑、暂停和恢复提供了控制依据。

**不足**是只有目标和完成状态，往往不足以保持完整的任务理解。如果每次执行只收到同一句 Goal，却不知道约束、进展、失败尝试和已确认决策，就可能重复探索、重新解释需求，或者把任务越做越大。

需要区分两类失败：目标漂移是成功标准或任务范围逐渐改变；死循环则可能发生在目标完全不变的情况下，例如不断尝试同一种无效修复。仅仅重复目标，不能解决后者。

### 2. 扩展方案：维护共享任务状态

对于 Coding 等复杂任务，可以在 Goal 之外增加以下信息：

| 信息 | 作用 |
|---|---|
| Goal | 明确最终要达到的结果 |
| Constraints | 保存范围、接口兼容性和用户明确要求等边界 |
| Acceptance criteria | 明确什么证据能够证明完成 |
| State | 记录已完成工作、已确认事实、当前问题和关键决策 |
| Evidence | 保存测试结果、代码位置、失败尝试及原始记录引用 |
| Next actions | 记录近期准备执行的动作和需要验证的假设 |

严格说，这是一份共享任务状态；外层状态机负责决定何时执行、验证、重新规划或停止。续跑时读取最新状态，可以减少不同执行者仅凭目标文本重新理解任务的问题。

不过，保存状态并不等于能够判断进展。同一动作反复产生相同失败时，只有历史记录仍可能继续死循环；增加进展判断和重规划机制可以帮助识别这类问题，但也会增加控制逻辑的复杂度。

**优势**是提高跨轮次、跨执行者的任务一致性，减少遗漏约束和重复劳动，也便于判断任务是否真正完成。

**代价**是状态维护、同步和校验的成本。状态更新不及时，或混淆工具确认的事实、模型推测和已经废弃的方案，都可能误导后续执行；多个 Agent 共同写入时，还存在版本与冲突问题。模型自报的完成状态也可能与测试或产物验收结果不一致。

这里需要区分“状态详细”和“流程固定”：Goal、Constraints、State 等通用字段更容易跨领域复用；过度限定状态的表达方式，或把“修改代码 → 单元测试 → 提交 PR”等具体流程写死，都可能限制泛化。相应的权衡是：**状态过少会损失连续性，状态过多会增加维护负担，过度绑定领域的结构和流程会限制适用范围。**

## 二、Todo 与计划提醒：让模型持续看到当前进度

Todo 工具将任务分解和进度保存为结构化清单。模型在执行过程中调用工具，新增事项或更新 `pending`、`in_progress`、`completed` 等状态。Harness 再根据策略，把当前清单重新提供给模型，或者提供读取工具供模型主动查询。

Claude Code 的两套工具可以说明这种区别：

- 旧版 `TodoWrite` 负责写入完整清单；Harness 可以在满足提醒条件时读取清单，自动将其放入后续上下文。
- 新版 Task 工具允许模型通过 `TaskList` 读取列表，通过 `TaskGet` 获取详情；本次检查的实现也保留了自动提醒路径。[官方任务跟踪说明](https://code.claude.com/docs/en/agent-sdk/todo-tracking)

自动提醒不需要模型另行发起读取调用。它由 Harness 将当前清单格式化为消息，加入后续模型输入。Todo 本身主要提供计划与进度信息，不负责在一次 Run 结束后自动启动下一次 Run。

**潜在收益**是让多项交付要求和当前进度显式化，帮助检查事项遗漏，并让用户观察执行过程。配合明确引导和状态回放时，模型可能更早建立计划、持续维护当前事项。但这些过程变化不自动意味着目标漂移更少、实现质量更高或任务成功率提升，需要分别评估过程一致性、最终验收结果和执行成本。

**主要风险是计划过时和规划僵化。** 如果一开始就生成很长的 Spec、详细 Plan 和大量步骤，并反复把它们作为强指导注入，模型可能过度依赖最初的设计。新证据已经说明方案不适用，它仍可能继续完成旧清单。反复注入还会增加 Token 开销，并使过时计划与当前事实竞争模型的注意力。

这不是 Todo 工具不允许修改计划。TodoWrite 可以重写清单，新版 Task 工具也允许修改和删除任务。问题在于模型是否会及时识别计划失效，以及 Harness 是否持续强化旧计划。

滚动规划是另一种权衡：保留粗粒度里程碑，只详细安排近期动作，可以降低过早固定方案的风险；代价是持续调整计划的开销，以及远期依赖没有被充分展开时的遗漏风险。详细计划与滚动规划各有成本，不能脱离任务结构判断哪一种更好。

随着模型能力提升，强制显式规划和频繁提醒的收益也可能下降。Claude Code 当前文档已经说明，部分较新模型默认不再提供任务跟踪工具，应用需要读取进度时仍可启用。这支持按模型和任务选择策略，但不能直接证明 Todo 对所有长程任务都没有价值。[模型与任务工具的可用性](https://code.claude.com/docs/en/agent-sdk/todo-tracking#model-availability)

同样，不能把 Codex 的计划工具简单理解为“只有 UI 作用”：即使没有固定间隔的清单提醒，模型调用计划工具时提交的步骤和状态仍会进入对话记录，可能影响后续执行。进度展示与上下文作用可以同时存在。

### 实验案例：Todo 改变执行行为，但结果收益尚不确定

[Apache Maka PR #561](https://github.com/apache/maka/pull/561) 报告了一组 headless/Harbor 任务工具实验。以下数据来自 PR 的公开实验摘要，本文未复跑实验。在默认启用 active prune（上下文裁剪）的设置下，各方案分别与关闭任务工具的对照组比较：

| 实验方案 | 实际调用任务工具的尝试数 | 对照组通过数 → 实验组通过数 | 实验组成本变化 |
|---|---:|---:|---:|
| CRUD-lite | 1/8 | 5/8 → 4/8 | +65.3% |
| 仅 `todo_write` | 2/8 | 5/8 → 3/8 | +15.0% |
| `todo_write` + 引导与状态回放 | 7/8 | 4/8 → 5/8 | +18.8% |

每行都是各自的 A/B 比较，不能把三行视为共用同一次对照运行。工具使用比例的分母包括启用了工具但未调用的尝试，并非只统计使用过工具的运行。

加入引导后，首次任务工具调用位置的中位数为第 1 次工具调用，部分成功轨迹中出现了持续更新清单直至最终验证的行为。但这一组通过数的提升只来自 `large-scale-text-editing`；`overfull-hbox` 仍出现清单标记完成、实际验证失败。每个实验组只有 8 次尝试，CRUD-lite 和引导组的报告还各列出 4 个轨迹缺失项，因此不能据此宣称成功率稳定提升。该 PR 保留的方案仍是默认关闭的实验功能。[实验结果与限制](https://github.com/apache/maka/pull/561)

这里的处理条件也不只是“增加一个 Todo 工具”。实现通过 `turnTailPrompt` 提供当前清单和明确引导，要求长任务开始时建立简短计划、保持一个进行中事项，并在计划变化时重写清单。因此，观察到的变化应归于**工具、使用引导和状态回放的组合**，不能单独归因于清单存储。[引导与回放实现](https://github.com/apache/maka/blob/586581fab1d05ca66ba69f29f71d28d5a9121538/packages/headless/src/task-ledger-experiment.ts)

这组实验支持的结论是：**Todo 配合 Harness 引导可以改变长程任务的执行行为，但行为更有规律不等于实现效果更好。** 如果要进一步声称“行为稳定性提高”，还应通过重复运行检查目标偏离、遗漏和执行波动是否减少，而不能只看清单更新次数。结果没有改善，也不足以证明原因就是规划僵化。

## 三、Subagent 与 Handoff：隔离或交接执行上下文

这两种方式可以放在一起讨论，因为它们都涉及将任务及其背景传递到另一个执行上下文，只是承担的工作不同：

- **Subagent**：主 Agent 保留总目标、约束和关键决策，把搜索、排查或局部验证等子任务交出去。子 Agent 在自己的上下文中处理文件、日志和试错，再返回必要结论与证据。
- **Handoff**：当主任务的上下文过长或积累了过多无关过程时，把当前任务状态整理为交接包，交给新的 Session 接续主任务。

**共同优势**是控制单个上下文需要承载的信息量，减少局部过程对目标和约束的干扰。Subagent 还可以并行处理独立子任务；Handoff 则帮助主任务跨窗口继续。

**共同代价**是信息选择、理解恢复和结果整合。传递内容过少，可能遗漏约束、决策依据或失败尝试，导致重复工作或重新解释任务；传递内容过多，又会把原有上下文负担带入后续执行，削弱隔离或换窗的收益。

更重要的共同风险是**继承主 Agent 的错误理解或判断，导致后续执行持续跑偏**。如果主 Agent 对需求、问题原因或完成标准理解有误，并将这些判断写入委派消息或交接包，子 Agent 或接手任务的 Agent 就可能把错误前提当成事实继续推进。即使信息传递完整，这种错误仍可能被保留下来，并在多次委派或交接中逐渐固化。多个执行者共享同一错误前提时，结论一致也不能作为相互独立的验证。

保留原始需求、证据和假设标记，可以为后续执行者重新核验前提提供依据，但也增加了读取、理解和验证的成本。上下文更简洁与任务理解更完整之间存在权衡。

具体继承范围取决于实现。例如，Claude Code 的普通子 Agent 通常从独立上下文和委派消息开始，而 fork 可以继承父对话。即使没有继承完整历史，委派消息也可能传递主 Agent 的错误判断。[官方子 Agent 说明](https://code.claude.com/docs/en/sub-agents)

## 四、各类方案的收益与代价

| 机制 | 主要解决的问题 | 主要风险或成本 |
|---|---|---|
| 外部 Goal 与任务状态 | 跨执行保持目标、进度，支持续跑和恢复 | 状态不足、不同步、验收失真 |
| Todo 与计划提醒 | 显式跟踪当前事项与进度，帮助检查遗漏 | 上下文开销、清单过时、计划僵化，结果收益不确定 |
| Subagent / Handoff | 隔离局部探索或交接主任务，控制上下文规模 | 信息损失、理解恢复与整合成本、继承错误判断后持续跑偏 |

这些方案的收益都带有条件：外部状态依赖记录的准确性和及时性，Todo 的价值取决于计划是否仍然有效，Subagent / Handoff 的效果受任务可拆分程度和信息传递质量影响。过程更有条理、上下文更短，都不能直接替代完成率、约束违反率、重复执行次数和成本等结果指标。

---

实现核对说明：本文中的 Claude Code 自动提醒细节来自本机 2.1.187 安装包的检查；该版本按有效 assistant 消息数检查提醒条件，并非按墙钟时间定时。Codex 计划工具的核对版本为 0.151.0。具体工具可用性和提醒策略会随版本、模型与配置变化；以上架构分析不依赖某个固定提醒间隔。

---

## 笔试作答版（模块三第 1 题）

“忘记目标”可能有不同原因：目标和约束被大量工具结果淹没；压缩或跨 Session 交接丢失状态；模型陷入局部问题，偏离总目标；或者新证据出现后仍在执行旧计划。常见方案有以下三类，它们的收益和代价各不相同。

**1. 在 Agent Loop 外保存 Goal 和共享任务状态**

基础做法是持久化目标及运行状态，由外层控制逻辑在一次 Run 结束后判断是否续跑，续跑不要求新建 Agent 或清空上下文。在此基础上，还可以扩展为共享任务状态，记录约束、验收标准、进展、关键决策和失败尝试。

优势是保持跨轮次的任务连续性，减少重复探索，为暂停、恢复和验收提供依据。代价是状态维护和同步成本：记录太少容易遗漏信息，记录太多会增加维护负担；状态过时或记录有误还会误导后续执行。过度限定状态结构或写死领域流程也可能限制泛化。只有目标和状态，并不能自动解决连续无效尝试或死循环。

**2. 用 Todo 和滚动规划维护近期进度**

通过 Todo 工具显式记录待办及状态，由 Harness（执行框架）按策略回放当前清单，或让模型主动读取。优势是让当前事项和进度可见，帮助检查遗漏；代价是维护清单和提醒的 Token 开销，以及过时计划被反复强化的风险。滚动规划只细化近期动作，能降低过早固定方案的风险，但也增加了持续调整计划的成本。Todo 本身不负责 Run 结束后的自动续跑。

需要区分行为变化与结果改善。[Apache Maka PR #561](https://github.com/apache/maka/pull/561) 的公开实验中，`todo_write` 配合引导和状态回放，在 7/8 次尝试中被调用，并出现持续维护清单的行为；但该组通过数仅从对照组的 4/8 变为 5/8，成本增加 18.8%，还存在清单完成而验证失败的案例。小样本和轨迹缺失限制了结论，而且测试的是工具与引导的组合。因此，不能把清单更新更有规律直接当作稳定性、实现质量或成功率提高的证据。

**3. 用 Subagent / Handoff 隔离或交接执行上下文**

这两种方式都把任务及相关背景传递到另一个执行上下文。Subagent 承接搜索、排查等局部任务，返回结论和证据，主 Agent 继续负责整体推进；Handoff 则通过交接包，让新的 Session 接续主任务。它们可以减少单个上下文的负担，代价是信息损失、理解恢复和结果整合成本。

两者共同的风险是继承主 Agent 的错误理解或判断。如果主 Agent 已经误解需求、判断错了问题原因，却把这些判断作为委派或交接的前提，后续 Agent 就可能沿着错误方向继续执行。即使信息传递完整，错误也可能被持续保留；多个执行者沿用同一前提得到一致结论，也不能证明这个前提正确。

这类方案的权衡在于信息传递的完整性与上下文开销：传得太少容易丢失约束和依据，传得太多又会保留原有负担。保留原始需求和证据有助于核验错误前提，但也会增加后续读取与验证的成本。
