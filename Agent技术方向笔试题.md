# Agent 技术方向笔试题

恭喜你通过我们的一轮面试！以下是我们 Agent 技术方向的笔试题。

本次笔试共分为两部分：**Vibe Coding 题目**和**架构设计题目**。

## 注意事项

1. Vibe Coding 题目请注意实现细节。
2. 架构设计题一共包含 5 个模块，**每个模块选择一道题目回答**。
3. 架构设计题中，字数多不代表思考深入。请使用大模型帮助你思考，而不是让大模型代替你完成任务。人与人之间的差距，在于如何使用 AI 帮助自己完成工作。

---

## 第一部分：Vibe Coding——从零实现一个最小可用 Agent

### 要求一：从零完成

- 不能依赖现有 Agent 框架（如 LangGraph、OpenHands、OpenClaw）完成主流程。
- 允许使用任何 AI 工具辅助开发，但核心 Agent Runtime 需要自行实现。

### 要求二：实现基本循环

#### Loop 大致步骤

1. 接收用户输入。
2. 判断是直接回复，还是调用工具。
3. 调用工具。
4. 根据工具结果判断是继续 Loop，还是向用户返回最终结果。

#### 工具相关

至少实现三个工具：

- `calculator`
- `search`（可使用 Mock 实现）
- `read_docs`、`todo`、`weather` 等工具中的任意一种，也可自定义其他工具

同时需要满足以下要求：

- 实现工具注册机制。每个工具应包含：
  - 名称
  - 描述
  - 参数 Schema
- LLM 能够基于工具 Schema 自主决定是否调用工具以及调用哪个工具。
- 实现 LLM 输出解析逻辑，能够提取：
  - 思考过程
  - 工具调用
  - 最终答案

#### Session 管理

假设存在以下场景：

- 用户 A 在窗口 1 中让 Agent 添加日历事项。
- 用户 A 在窗口 2 中让 Agent 添加联系人。

两个窗口应对应相互独立的 Session。用户 A 可以随时回到窗口 1 或窗口 2 继续对话，且两个 Session 之间不会互相影响。

#### Context 的有效管理

需要考虑并实现以下能力：

- 设置最大 Loop 轮次限制。
- 在持续对话中记住之前的状态。
- 支持追问：
  - 纯对话追问
  - 需要继续调用工具的追问
- 判断哪些信息适合放入 Context，例如：
  - 用户输入
  - 工具执行结果
  - Agent 思考过程
  - 其他必要状态
- Context 过长时，需要实现基础压缩；本题不要求实现复杂的压缩方案。

#### 额外要求

- 基本异常处理
- 工具调用 Trace 或执行日志

### 要求三：测试用例构建

构建测试用例，覆盖并验证以上功能。

### 提交内容

- 使用真实的 LLM API。
- 代码链接（GitHub 链接即可）。
- README，至少包含：
  - 运行方式
  - 系统设计
  - Memory 的召回时机
  - Memory 的放置方式
- AI Prompt 与问题解决记录。

---

## 第二部分：架构设计题

> 共 5 个模块，**每个模块选择一道题目回答**。

### 模块一：Context / Performance

1. 大模型面对第一轮长窗口或多模态输入时，首 Token（First Token）响应会显著变慢。有哪些快速、低成本且用户体验较好的方案，可以将响应时间从 5～10 秒稳定压缩到 2 秒？
2. 一个 Session 已经连续对话 200 轮，Context 即将达到上限。你会如何进行压缩？如何确保压缩后的对话仍然流畅？

### 模块二：Memory

1. 用户与聊天 Agent 相处半个月后，再次询问一个以前问过的问题。Agent 应如何进行更合理的 Memory 召回？
2. 你所理解的 Agent Memory 经典框架是什么？它的发展趋势是什么？行业头部玩家正在采用哪些方案？

### 模块三：Task

1. 对于长程任务，大模型执行一段时间后可能会忘记目标。你知道哪些解决方案？各自有什么优缺点？
2. 用户向 Agent 下达任务：“每天早上 9 点，根据昨天的聊天情况生成复盘总结。”你会如何设计这一功能？

### 模块四：Tool / Session Runtime

1. Agent 工具有同步和异步两类。异步工具不能让用户一直等待，但其执行结果仍然重要。你会如何设计异步工具的执行和完成通知机制？
2. 如果 Session State 为 `busy`，此时用户又发送了新消息，或者异步工具完成事件到达，Runtime 应如何处理？

### 模块五：Agent Runtime 架构对比

1. Claude Code 的工具输出方式，与国内 GLM、豆包等使用的 OpenAI-compatible Function Calling 有什么不同？这些设计各自有什么优缺点？
2. OpenHands 的状态机设计有什么优缺点？你认为更优雅的实现方式是什么？
