# 长 Session 的 Context 压缩设计

**1. 根据产品、场景和用户需求设计策略。**

Coding Agent 的文件读取、日志和工具结果，可能几轮交互就占满 Context，与普通长对话不同。压缩策略应结合具体产品、使用场景和用户需求，决定保留什么、如何压缩、何时触发。

**2. 分别保护历史背景与当前意图。**

History Context 要保证语义连续性和任务一致性：前者保留人物、指代、关系、情绪与因果，后者保留目标、约束、决策、执行状态和下一步。陪伴聊天更重视前者，Coding Agent 更重视后者。

Active Turn 单独保证当前意图保真，保留最新用户原文、结构化意图和本轮关键执行状态。最新要求可能修改或替换旧任务，不能被历史摘要覆盖；原文与意图冲突时，以原文重新解析。

**3. 按信息特点选择保留、压缩或外置。**

综合信息的精度、风险、相关性、可恢复性等属性处理：当前工作集直接保留；任务状态做结构化压缩；一般历史做语义摘要；人物关系按需存储与召回；代码、日志等大块内容外置，保留关键片段和引用。

金额、授权等精确信息不能有损压缩，但可无损外置，携带来源与确认状态，在相关任务或高风险操作前召回校验。信息优先级随任务变化，已完成过程可降级，被替换决策应标记失效，推测与确认事实分开保存。

**4. 用分层 Context 和原始记录保证连续性。**

压缩后组合：当前原文与意图、最近若干轮原文、历史结构化状态、语义摘要和原始记录引用。原始消息、工具调用与结果持久化为 Event Log，并通过稳定 ID 关联。遇到指代不清、状态冲突或需要精确细节时，按需检索原文。

**5. 在调用前检查预算，溢出时按事件恢复。**

每轮首次及 Agent Loop 内每次调用模型前，都检查 Token Budget，综合窗口上限、当前输入、预留输出、预计工具结果和安全余量，必要时压缩。工具结果可外置；沿用原始调用协议时，调用与结果须合法配对。

Overflow 是兜底路径：从 Checkpoint 与 Event Log 恢复到最后成功持久化的事件，保留已完成动作，从失败的模型调用重试。有副作用的工具通过幂等键避免重复执行。

**6. 具体参数通过 Evals 确定。**

摘要字段、保留比例、触发阈值、召回时机和压缩强度，都应按业务评估。关注意图与约束保留、指代解析、任务完成、工具正确性、检索与事实核验，以及溢出率、Token 成本和响应时间。
